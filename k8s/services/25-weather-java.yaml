apiVersion: v1
kind: Service
metadata:
  name: doodle-weather-java
  namespace: supreme-doodle
spec: 
  selector:
    application: helloworld
    service: doodle-weather-java
  type: ClusterIP
  ports:
  - name: http
    # protocol: TCP
    appProtocol: http
    port: 5100
